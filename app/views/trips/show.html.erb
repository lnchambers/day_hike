<%= @trip.name %>
<% @trip.trails.each do |trail| %>
  <%= trail.name %>
  <%= trail.length %>
  <%= trail.address %>
<% end %>
Total Hiking Distance: <%= @trip.total_distance %>
Average Distance: <%= @trip.average_distance %>
